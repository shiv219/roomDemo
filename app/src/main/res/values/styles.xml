<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="customMaterialTextInputLayout" parent="Base.Widget.MaterialComponents.TextInputLayout">
        <item name="materialThemeOverlay">
            @style/ThemeOverlay.MaterialComponents.TextInputEditText.OutlinedBox
        </item>
        <item name="theme">@style/Theme.RoomDemo</item>
        <item name="boxCollapsedPaddingTop">0dp</item>
        <!-- The color of the label when it is collapsed and the text field is active -->
        <item name="hintTextColor">@color/black</item>
        <!--        <item name="boxStrokeColor">@color/text_input_box_stroke</item>-->
        <!-- The color of the label in all other text field states (such as resting and disabled) -->
    </style>

    <style name="edtStyleCommon" parent="Widget.AppCompat.EditText">
        <item name="android:background">@null</item>
        <item name="android:textSize">@dimen/text_size_regular</item>
        <item name="android:textColor">@color/home_competition_creater_text_color</item>
        <item name="android:textColorHint">@color/home_competition_creater_text_color</item>
        <item name="android:padding">@dimen/input_edit_text_padding</item>
    </style>


    <style name="Vehicle.Button" parent="Widget.AppCompat.TextView">
        <item name="android:padding">@dimen/padding_16</item>
        <item name="android:background">@drawable/bg_button</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/text_size_medium</item>
        <item name="android:elevation">5dp</item>
        <item name="android:foreground">?attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="FullScreenDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowAnimationStyle">@style/FullScreenDialogAnim</item>
    </style>

    <style name="FullScreenDialogAnim">
        <item name="android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_out</item>
    </style>
</resources>